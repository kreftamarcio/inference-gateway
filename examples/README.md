# Examples

These examples use `MockProvider`. They prove routing, budget, and streaming
without spending tokens.

```bash
npx tsx examples/01-cost-routing.ts
```

| File | What it proves |
|------|----------------|
| `01-cost-routing.ts` | Cost-aware selection, budget recording, streamed deltas |
